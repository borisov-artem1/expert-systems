# 🧠 Экспертная система — прямой вывод (Forward Chaining)

## 📖 Описание
Данный проект реализует **продукционную экспертную систему** с **прямым (дедуктивным) поиском от данных к цели**.  
Система принимает:
- **базу знаний** в виде набора правил (`Rule`),
- **входные факты** (`Node`),
- **целевую вершину** (`goal_node`),

и пытается **доказать** достижимость цели, пошагово применяя правила, у которых все входные вершины известны (закрыты).

Визуализация графа выполняется через **Graphviz**.

---

## ⚙️ Структура проекта

.
├── data.py # Описание вершин и правил (база знаний)
├── items.py # Определение классов Node, Rule, Label
├── search.py # Алгоритм прямого вывода (forward chaining)
├── graph.py # Построение графа зависимостей через Graphviz
├── main.py # Точка входа в программу
├── docs/ # Папка для сохранения визуализаций (.gv, .pdf)
├── requirements.txt # Зависимости проекта
└── .gitignore


---

## 🧩 Пример работы

### Пример входных данных (`data.py`)

```python
RULES = [
    Rule(101, NODE_5,  [NODE_1,  NODE_2]),
    Rule(102, NODE_11, [NODE_3,  NODE_4, NODE_5]),
    Rule(103, NODE_13, [NODE_5,  NODE_6, NODE_19]),
    Rule(107, NODE_17, [NODE_11, NODE_12, NODE_13]),
    ...
]
